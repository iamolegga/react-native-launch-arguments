{
  "name": "example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "start": "react-native start",
    "lint": "eslint .",
    "detox:ios:build": "detox build -c ios",
    "detox:ios:test": "detox test -c ios",
    "detox:ios": "npm run detox:ios:build && npm run detox:ios:test",
    "detox:android:build": "detox build -c android",
    "detox:android:test": "detox test -c android",
    "detox:android": "npm run detox:android:build && npm run detox:android:test"
  },
  "dependencies": {
    "react": "17.0.2",
    "react-native": "0.68.2",
    "react-native-launch-arguments": "file:./react-native-launch-arguments-3.0.1.tgz"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9",
    "@babel/runtime": "^7.12.5",
    "@react-native-community/eslint-config": "^2.0.0",
    "babel-jest": "^26.6.3",
    "detox": "^19.7.1",
    "eslint": "^7.32.0",
    "eslint-plugin-detox": "^1.0.0",
    "jest": "^28.1.1",
    "metro-react-native-babel-preset": "^0.67.0",
    "react-test-renderer": "17.0.2"
  },
  "jest": {
    "preset": "react-native"
  }
}
